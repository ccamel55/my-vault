syntax = "proto3";

package bw.user;

message EchoRequest {
  string message = 1;
}

message EchoResponse {
  string message = 1;
}

service Echo {
  // Echo what ever was send
  rpc Echo(EchoRequest)
      returns (EchoResponse);
}